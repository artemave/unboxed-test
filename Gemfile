source 'https://rubygems.org'

gem 'rest-client'

group :test do
  gem 'cucumber'
  gem 'aruba'
  gem 'rspec'
  gem 'rest-assured' # just showing off; vcr would be a better (faster) fit
  gem 'sqlite3'
end
